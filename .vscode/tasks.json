{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "json-server",
      "type": "shell",
      "command": "yarn && yarn json-server --host 0.0.0.0 server.json -p 3333"
    },
    {
      "label": "test",
      "type": "shell",
      "command": "yarn && yarn test"
    },
    {
      "label": "start",
      "type": "shell",
      "command": "yarn && yarn start"
    },
    {
      "label": "run:android",
      "type": "shell",
      "command": "yarn && yarn android"
    }
  ]
}
